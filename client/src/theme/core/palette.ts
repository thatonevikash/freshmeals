// -------------------------------------------------------------

const basePalette = {};

const lightPalette = {
  ...basePalette,
};

const darkPalette = {
  ...basePalette,
};

// -------------------------------------------------------------

export const colorSchemes = {
  light: { palette: lightPalette },
  dark: { palette: darkPalette },
};
